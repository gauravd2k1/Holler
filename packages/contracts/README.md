# packages/contracts

Source of truth for all cross-boundary shapes (TS+Zod, Go structs, OpenAPI, JSON Schema events, SQLite/Postgres migrations). See ADR-008 and CLAUDE.md.

**Empty by design at Milestone 0.** Populated and then frozen at **Milestone 0.5** (see HOLLER_MASTER_PROMPT.md §81). Builder agents treat this directory as read-only once 0.5 lands; only the orchestrator/architect session edits it.
