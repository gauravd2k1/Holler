# pnpm 10+ reads build-script approval here, not from package.json.
# vitest's esbuild binary must be allowed to run its postinstall or the KDS
# test suite cannot start.
allowBuilds:
  esbuild: true
