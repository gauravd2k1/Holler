# Holler backlog — the single register

**This is the only backlog.** One table, one row per deferred item. It replaced four
overlapping registers on 2026-08-29 (`docs/RESUME.md` §6, `docs/backlog-m2.md`,
scattered ADR "filed" notes, and the M5 planning triage). `docs/backlog-m2.md` is
deleted; `RESUME.md` §6 and `m5-planning.md` §1.6 now point here.

Two rules, because four registers is how an item gets triaged twice and scheduled
never:

- **A deferred item lives here and nowhere else.** ADRs keep the *reasoning* for a
  deferral — that is what an ADR is for — but the *status* is this table. If they
  disagree, this table is wrong and should be fixed, not forked.
- **Triage files an item; it does not schedule it.** A landing milestone here is a
  filing, not a commitment, until it appears in that milestone's track graph in
  `CLAUDE.md`.

**Not in this register:** M5's own scope and acceptance criteria (`CLAUDE.md`), and
anything already closed (closed items stay struck through in git history, not here).

| Item | Why it matters | Landing / trigger | Where it was found |
|---|---|---|---|
| **`edge/sync` HAS NO HOST — nothing that ships calls it, in either direction** | **The crate is complete, well tested, and wired to nothing.** No `Cargo.toml` in the repository depends on `holler-edge-sync`: `apps/pos/src-tauri` takes `holler-edge-database`, `-printer` and `-device` and not sync. `SyncWorker::new` is constructed nowhere outside tests. The only binaries in the edge/POS tree are `crashpoint`, `devseed` and `kds_lan_server`. `config::pull_and_apply_config` has exactly ONE caller: `edge/sync/tests/worker_integration.rs:433`. **So the config pull (cloud→edge) and every replay stream (edge→cloud) can only be made to happen by a test.** This is "a column nothing reads is a column that does not exist" at crate scale, and it is why both halves could pass CI for two milestones while no row ever crossed the wire outside a test process. It also means **M5 criterion 6 cannot be met as written** — the criterion forbids test-harness evidence, and a harness is the only thing that can currently trigger a replay. | **Blocks M5 acceptance criterion 6, and any pilot. Needs a decision on WHERE the worker is hosted** (POS Tauri process vs. a separate edge service) before it can be scheduled | This session, 2026-08-31; found by the wipe-and-sync condition on acceptance step 1 |
| **The inventory config push has never moved a row** | `inventory_item` is CLOUD→EDGE config: it is in `inventory.ConfigBundle` and ships as `inventory_items` on `GET /sync/config` (`backend/cmd/api/syncconfig.go:239`). But `backend/cmd/devseed` seeds tenant/brand/outlet/cashier/**menu** and NO inventory, while `edge/database/src/bin/devseed.rs` seeds 32 items, 5 conversions, 24 recipes and 93 ingredients straight into SQLite under its own deterministic ids. Postgres holds **zero** `inventory_item` rows for the seeded outlet — every row in that table belongs to throwaway integration-test outlets. So the cloud has always sent an empty `inventory_items` array to an edge that looked fully stocked. **M4's acceptance never touched this path**: criterion 1's "real seed menu" was the EDGE's seed, not config that travelled. That does not invalidate M4's deduction results, which are about what the edge computes from rows it holds — but **the claim "the cloud owns inventory config and the edge receives it" has never been demonstrated, across two milestones.** Consequence for M5: `supplier_item.inventory_item_id` has nothing valid to reference in the cloud, and the cloud is the side that must reject a `quantity_dimension` mismatch against `inventory_item.dimension` — it cannot compare against a row it does not have. | **M6** — and note it cannot be verified before the `edge/sync` host question above is answered, since proving arrival requires an empty start and a real pull | This session, 2026-08-31; found staging acceptance step 1 |
| **No periodic sync pump while the till is open — PILOT BLOCKER** | ADR-020 landed the drains at both ends of a trading day and deliberately not a timer. **Frame the gap as EXPOSURE, not staleness.** A till open 11am-midnight whose uplink returns at 2pm keeps a **full trading day** of orders, invoices, payments and ledger entries in **one encrypted file, on one spinning disk, with no off-machine copy**, until closing. One disk failure at 11pm loses the day — and ADR-013's hardware is exactly the hardware that fails that way. Replay is not a freshness feature here, it is the only thing that gets the day off the machine. **A timer calling the already-bounded `AppState::drain_outbox` is small; the exposure is not.** NOT an M5 blocker: criterion 6 is outbound and the shutdown drain satisfies it. | **Trigger: before the first outlet runs unattended** — i.e. any pilot | ADR-020, 2026-09-01 |
| **No surface lists goods receipts or their totals** | The only procurement reads are `list_grn_gaps`, `purchase_order_receipt_progress`, `grn_entry_intent_echo` and `weighted_average_cost_paise`. `Db` has no `list_goods_receipt_note` at all, and no route reaches one. **A costing figure whose inputs cannot be read back in-product is unauditable by the person who owns the money** — during M5 acceptance the three receipts behind a weighted average had to be read with `sqlite3` against the live edge file, which no operator can do. Goes with `apps/admin`, where a receipts list belongs. | **M6**, with `apps/admin` | M5 criterion 7, 2026-09-01 |
| **Nothing distinguishes a tax-inclusive from a tax-exclusive purchase price at entry** | `purchase_price_paise` is whatever the operator types, and in this market the delivery-note figure is commonly GST-INCLUSIVE. Typing it lands an inflated cost on every ledger row, silently. Doing it properly needs input-tax-credit handling and belongs with the compliance track, not procurement — **ADR-pending**, no shape proposed here. Mitigated now at zero cost by labelling the field "Price per <unit>, before GST": it enforces nothing, but it turns a silent mis-costing into an operator-visible error. | **Trigger: the compliance track opens, OR before the first pilot — whichever is first** | M5 criterion 7, 2026-09-01 |
| **Cost is a LIFETIME cumulative purchase-weighted average, not weighted average cost of stock on hand** | Stated in those words in ADR-021 §5. The averaging query is UNBOUNDED: outlet, item, non-null total, positive quantity, and nothing else — no `through_entry_seq`, no `business_date`, no on-hand term. Twelve sealed `stock_balance_snapshot` rows carry exactly the marks the balance path bounds its reads with, and the cost path references none of them. **Stock bought at old prices drags the figure permanently while the outlet pays current prices**, and an owner reading "average cost" generally means the value of what is on the shelf. **Only half of this was decided:** excluding outbound rows is argued in `procurement/cost.rs`; the unbounded-over-all-time property is recorded nowhere — "on hand", "moving average" and "periodic average" appear in no design document. Deliberately NOT folded into 0.6.3, which fixed the arithmetic and did not touch which rows are summed. **Cheaper than it sounds:** `stock_balance_snapshot.through_entry_seq` is the bound an on-hand WAC would read from and already exists. Note criterion 7 is definition-neutral as written and cannot detect which definition is implemented. | **Trigger: before the first pilot at the latest** — food costing is a headline claim | ADR-021, 2026-09-02 |
| **A 500 on a replayed row WEDGES THE OUTBOX FOREVER — pilot blocker** | Observed live 2026-09-02. `POST /orders/{id}/items` replay returns **500** on `order_item_menu_item_id_fkey` (SQLSTATE 23503). Two faults stacked. **(a) The cloud reports a client-data problem as a server fault:** an FK violation on a replayed row is a PERMANENT referential failure, but it falls through `httpx: unhandled error` and comes back 5xx. **(b) The edge classifies 5xx as TRANSIENT** (`edge/sync/src/procurement.rs:41` — "charges nothing"), so the row never spends retry budget, never reaches `sync_replay_block`, and retries forever. **And the general outbox has NO per-entry budget at all** — it stops in order, so ONE unreplayable row strands every row behind it. Observed: 120 pending, `max_attempts = 5`, drain stopped `Rejected { status: 500 }`. **This is exactly the silent-halt outage 0.5.8's per-entry budget exists to prevent, defeated by a status-code mapping nobody wrote.** Fix is both halves: map referential failures to 4xx cloud-side, and give the general outbox the per-entry budget the procurement and ranged pumps already have. | **Trigger: before any pilot** — order replay is wedged permanently today | This session, live POS startup drain |
| **The cloud menu seed is a token, and the edge's is real — orders referencing it can never replay** | Cloud holds **2** `menu_item` rows for the seeded outlet (Chai, Thali); the edge seeds **43**. Root cause of the FK 500 above. **Identical in shape to the inventory-config gap already filed**: `menu_item` is CLOUD→EDGE config, only the edge ever seeded it in full, and nothing has moved a menu row across the wire either. The two rows together say the config push has never been exercised for EITHER catalogue. | **M6**, with the inventory config push — same fix, same decision | This session, 2026-09-02 |
| **The shutdown drain is the ONLY outbound path, and abnormal exit bypasses it — PILOT BLOCKER, raised from the periodic-pump row** | `RunEvent::Exit` fires on a normal Tauri shutdown. **Ctrl+C, taskkill, a crash and power loss do not deliver it**, so the drain never runs and the day never leaves the machine. Observed: an earlier POS run ended `STATUS_CONTROL_C_EXIT` and drained nothing. With no periodic pump, **a till that crashes once never syncs again until someone closes it cleanly — and it looks healthy the whole time**, because nothing surfaces an outbox that is not draining. This is a strictly stronger argument than the staleness case the periodic-pump row was filed on: that one loses a day's freshness, this one loses the day. **The periodic pump is the fix for both**; treat that row as raised in priority by this finding rather than as a separate item. | **Trigger: before the first outlet runs unattended** — same as the pump | Operator run, 2026-09-02 |
| **The receiving confirmation says "no purchase order was recorded" when it means "no PO resolved"** | The screen prints "No purchase order was recorded against this delivery" while also listing "Received against purchase order PO-CRIT5-001". The reference IS stored; what failed is resolution against a local `purchase_order` row. **A cashier reads that as the reference not saving**, and the two lines contradict each other on the same screen. Same defect class as every doc comment we have filed — a statement asserting something the system does not mean — but in user-facing copy, where the reader cannot check the source. | **M6** | Operator run, 2026-09-02 |
| **Should a late-night receipt belong to the business day that just ended?** | Observed correct outlet-local behaviour: a receipt at 01:58 local (2026-09-01T20:28:53Z) got business date 2026-09-02, and `outlet.day_start_time` drove it. **Question, not a defect:** restaurants usually count a late night as the prior trading day, and a delivery taken at 02:00 after a night service arguably belongs to the day that just closed. Depends on `day_start_time` being configured to the outlet's real trading pattern rather than midnight. **File, do not act** — needs an operator answer, not a code change. | **M6** — needs a product decision first | Operator run, 2026-09-02 |
| **`StopReason::Offline` collapses four different situations into one word** | Fixed the acute case (a dead pooled socket is now retried once before being believed) but NOT the reporting. The worker still says "found no route to the cloud" for: nothing listening, listening but refusing, a socket the peer already closed, and any unclassified transport error. **The word chosen blames the customer's network**, and an operator cannot act on it. `scripts/check-cloud-unreachable.ps1` already contains the discriminator — three independent probes, fail-closed, REACHABLE when a failure cannot be classified. **Reuse that logic rather than writing a second weaker version.** | **M6** | Operator observation + the false-offline run, 2026-09-02 |
| **`LanServerHandle::shutdown()` dials its own bind address, which is not dialable** | It wakes the blocked `accept()` with a dummy connection to `self.local_addr` — but that is the BIND address, and `0.0.0.0` cannot be used as a target: *"IPv4 address 0.0.0.0 and IPv6 address ::0 are unspecified addresses that cannot be used as a target address"* (proven 2026-09-02; `127.0.0.1:9310` connects fine). The dial fails, `if let Ok(..)` swallows it, and the accept thread is never woken by the mechanism intended to wake it. **It did NOT cause the shutdown hang I initially blamed it for** — the process exits, so something else unblocks accept — but a shutdown path whose wake-up is a silent no-op is a latent hang waiting for the right timing. Fix: dial loopback with the bound port, and do not rely on the dial at all — a read timeout or non-blocking accept notices the flag regardless. | **M6** | Found 2026-09-02 while misdiagnosing the exit path |
| **The error envelope's `code` is a free-form string the edge will branch on** | A1 puts `missing_reference` on the wire and the edge classifies on it, but the contract types the error body as `{ code: string, message: string }` with no enumeration anywhere — OpenAPI has `code: { type: string }` and nothing more, so a rename cloud-side is a silent behaviour change at the edge and no drift test can see it. Phase A is deliberately contract-free, so the enum cannot land with A1; filing it now so it is not rediscovered when the edge is already branching on three or four of these. Scope: enumerate every `code` the ingest routes can return — at minimum `missing_reference`, plus whatever the A1 sink audit adds — in OpenAPI, the Zod schema and the mirrored Go constants, with the edge's classifier reading the generated set rather than string literals. | **Contracts 0.7.0, after Phase A is green** — the version already scheduled for the aggregator shapes; no earlier trigger, since nothing before A1 emits a code the edge reads | M6 A1 planning, 2026-09-03 |
| **Ten ingest write paths still report a client-data failure as 500** | `docs/m6-a1-sink-audit.md` enumerates fourteen ingest-reachable write sinks. Four are settled -- `ordering`'s, classified and observed red-then-green at `99875cc` -- and two more (`kitchen.InsertKot`, `payments.InsertPayment`) are scheduled as **M6 A1b** because each has a named consequence. **These are the remaining ten**: `kitchen.UpdateKotStatus`, `payments.InsertInvoice` / `insertInvoiceLine`, `payments.InsertCashShift` / `CloseCashShift` / `insertMovement`, `inventory.InsertLedgerEntry`, `inventory.InsertStockCount`, `procurement.InsertGoodsReceiptNote`, `procurement.InsertPurchaseReturn`, `procurement.InsertStockTransferOut`, and `menu.UpdateItemAvailability`. Each wraps `pgconn.PgError` with `fmt.Errorf`, so a foreign-key violation on a replayed row returns 500 and the edge treats it as transient. **A2 shrinks the blast radius but does not fix them**: the aggregate still blocks, it just no longer strands its neighbours. Filed here rather than carried in the audit file, because a list of known defects living only in a document nobody re-reads is how they float. **Each needs its own red-then-green when fixed** -- the audit rows are Read-verified and nothing has observed any of them failing. | **M6 Phase A, after A1b** -- or the first time one of them is observed wedging a real outlet, whichever is first | `docs/m6-a1-sink-audit.md`, 2026-09-03 |
| **A wall-clock assertion in `stale_connection.rs:160` fails under load** | `a_genuinely_unreachable_cloud_is_still_reported_as_transport_failure` asserts the two-attempt path against a refused port completes in **under 30 s**. It **failed once** during a full `cargo test -p holler-edge-sync` run that took **218 s** with parallel cargo jobs on the same box; it **passed alone in 4.1 s** and passed in the next full run. **Test fragility, not a product defect** — the assertion is about our own retry budget, and the transport behaviour it guards is correct. But this is the same FILE M5 stabilised to 0/200 by fixing its fake servers, and this is a DIFFERENT assertion in it: a wall-clock bound on a loaded machine returns, and it returns as a mystery if nobody wrote the numbers down. Fix is to bound attempts rather than seconds, or to raise the bound with the reason recorded. **An unfiled flake becomes a bisect later.** | **Trigger: before the first unattended pilot run** — a flaky suite is how a real regression gets waved through | M6 A2 full-suite run, 2026-09-03 |
| **THREE block-and-budget mechanisms for one concept** | After M6 A3 the edge has three ways of saying "this row will not replay, stop trying and tell someone". **Ranged** (`LEDGER`, `DEDUCTION_GAP`): budget in `sync_replay_block.attempts`, durable row, `blocked_at` set -- `edge/sync/src/ranged.rs` is that table's ONLY writer. **Procurement**: budget in `local_outbox.attempt_count`, blocked entries reported **in memory only** as `BlockedProcurementEntry`, so the count survives a restart and the reason does not. **General outbox** (ADR-023): budget in `sync_outbox_block.attempts`, durable row. **Three mechanisms for one concept is drift**, and drift here is invisible until the day one of them abandons a row differently from the others. Deliberately NOT converged as part of A3: converging three mechanisms while fixing the outage one of them exists to prevent is how a fix acquires a second defect. Note the choice is not obvious -- procurement's blocked entries are a buyer-facing event acted on the same day and `grn_gap` carries the operational half, so "make procurement durable too" may be the wrong direction. | **Trigger: before the first pilot, OR when a fourth stream needs a budget -- whichever is first.** A fourth mechanism is the point at which this stops being tidiness | ADR-023, 2026-09-03 |
| **Batch/expiry alerting** (`grn_line.batch_code`, `expiry_date`) | Fields are captured at receipt or never, and both are EXEMPT in `check-contract-field-consumers.mjs` with M6 named. **Both exemptions come out when this lands** — an exemption that outlives its reason is a silenced failure. | **M6** | ADR-019 §8; M5 scope cut |
| **Four M1/M2 POS ordering defects** — DINE_IN accepts no table; cart does not clear after send and per-item controls ignore the non-amendable state; "Beverages" listed twice; internal "Kitchen Prep" category is orderable | Twenty minutes of manual clicking found all four. Every one is reachable from the shipped till today. | **M6** | `backlog-m2.md` "M4 manual POS pass", 2026-08-27 |
| **`PosScreen.tsx` never surfaces `isError`** | A failed menu query renders "Loading menu…" forever — `hydrate` runs only on `isSuccess`, so a rejection is indistinguishable from a slow load. This is why the Tauri DTO drift went unnoticed. | **M6** | RESUME §6 |
| **Cash-shift recovery mounts on `BillingScreen`, not app startup** | Shift recovery depends on which screen the cashier happens to open first. | **M6** | RESUME §6 (`apps/pos/src/store/cashShift.ts`) |
| **Guard-rail track: narrow the field-consumer corpus** (exclude doc comments and `#[cfg(test)]`) | **Ordering is load-bearing: narrow the corpus BEFORE extending to enums, or the check ships inert.** Measured — the six unwritten `entry_type` values appear only in a doc comment and one test fixture. | **M6** | RESUME §6.1; `M5_HANDOFF.md` 2.2 |
| **Guard-rail track: extend the field-consumer check to enum members** | It does not cover enum values at all. `is_default` appeared 3× in `model.rs` while no sale deducted stock — green check, broken product. Then declare the six `entry_type` values exempt with M5/M8 named. | **M6**, after corpus narrowing | RESUME §6.1 |
| **Guard-rail track: machine-check Tauri DTOs against the Zod schemas** | Nothing checks `apps/pos/src-tauri/src/dto.rs` against the contracts. `MenuItem` was missing `tax_profile_id`/`hsn_sac`; `MenuItemVariant` lacked `is_default`/`schema_version` — the 0.4.6 OpenAPI drift, same three fields, one layer out. Fixed 2026-08-27; **the guard is not**. | **M6** | RESUME §6 |
| **Guard-rail track: `make test` covers only Go** | The Rust and TypeScript suites are run directly, so the project's named test command does not run the project's tests. | **M6** | `backlog-m2.md` Testing |
| **NOTHING RUNS `cargo fmt` OR `cargo clippy` ON `apps/pos/src-tauri` — the whole crate, not just its tests** | Confirmed 2026-09-01, and it is wider than this row used to say. `edge-style` runs fmt and clippy on `edge/{database,sync,printer,device}` ONLY. `pos-style` is TypeScript only — `tsc` and `eslint`. `rust-seams` runs `cargo check --all-targets` on the crate, which checks neither. So the POS's entire Rust surface — nine command modules plus `state.rs`/`lib.rs`, the largest Rust surface outside `edge/` — drifts unchecked, and `commands/billing.rs` currently carries such drift. **The reason recorded for this no longer holds:** it was filed as "the crate is not built on Linux (ADR-013)", but `rust-seams` already builds it on `windows-latest`, so fmt and clippy could be two more steps in a job that exists. | **M6** | RESUME §6; re-scoped 2026-09-01 |
| **Append-only claim lint attributes by line distance** | A long table's claim lands on the *next* table; a triggers-only file's claim attaches to nothing and is **silently dropped**. 0027–0029 pass only because **the comments were written to suit it** — a documented obligation, not verification. The strict fix (prefer the table a claim NAMES) was prototyped and reverted: it surfaces pre-existing M1–M4 claims that each need a ruling. | **M6** | ADR-019; found landing contracts 0.6.0 |
| **HSN invariant folded into `9_tax_reconciliation`** | A tax arithmetic error and a missing compliance field share one invariant id, so the e2e verdict cannot say which failed. | **M6** | RESUME §6 (`tests/e2e-scenario`) |
| **`seed_offline_sale.rs` proves the resolver, not the caller** | It selects a variant directly; the POS passed `variantId: null` and the defect lived in that gap. **A deduction test proves deduction only for the path its caller takes.** Product fixed (`7e88d1c`); the missing test is not. | **M6** | RESUME §6; M4 criterion 1 |
| **`Db::connection()` is plain `pub`** | Three sibling crates hold it, so any of them can bypass the repository layer. `payment` is trigger-protected; `cash_shift` is not and cannot be (OPEN→CLOSED is a legitimate UPDATE). | **M6** | RESUME §6 (`edge/database/src/lib.rs`) |
| **`cancel_kitchen_items` has no Tauri command** | `#132-C` cancellation is unreachable from the shipped surface — the e2e harness exercises a path no operator can reach. | **M6** | RESUME §6 |
| **Postgres integration tests never clean up** | Every tenant/brand/outlet/order row stays forever. Harmless today, and the reason `TestBuildRouter_SyncConfigEndToEnd` needed a clean database. | **M6** | `backlog-m2.md` Testing |
| **Nothing machine-checks `openapi.yaml` against the handlers** | `check-openapi-go-drift.mjs` now closes SQL → Go/TS → OpenAPI **and runs in CI as of 0.6.0**. The remaining hop — spec vs. actual routes — is still unchecked. | **M6** | RESUME §6 |
| **`apps/admin` does not exist — the web admin application** | 0 files, never in git history. So are `packages/ui`, `packages/validation`, `apps/waiter`, `apps/customer-ordering`. M5's T5 was written as "add supplier + PO screens" and is actually "create the application, then add them" — ~30 files and a network `pnpm install`. **Building it from nothing is a milestone, not a track.** Until it lands, **purchase orders are raised through the API**, which is why T5a landed the list/update routes in M5. | **M6** | This session, 2026-08-30; found by T5 refusing to start |
| **Stock-transfer-out UI, and a screen for `purchase_order_receipt_progress`** | Both Tauri/edge sides exist and are tested; neither has a surface. The receipt-progress command is wired with no PO list to reach it from. Goes with `apps/admin`, since that is where a PO list belongs. | **M6**, with `apps/admin` | T4, 2026-08-30 |
| **`purchase_return.return_number` duplicates surface as a raw UNIQUE violation** | Contracts mint `grn_sequence` but no return counter, so the number is operator-typed. A duplicate gives a database error with no friendly message and no guidance. | **M6** | T4, 2026-08-30 |
| **`stock_ledger_entry.origin` has no procurement member** | Receipts, returns and transfers write `MANUAL` and carry provenance in 0027's three typed columns. It works, but "manual" is now a lie on every receipt row, and a report grouping by `origin` cannot separate a receipt from a hand adjustment. | **M6** — contract change, needs a version bump | T2, 2026-08-29 |
| **`grn_gap.grn_id` is NOT NULL, so a purchase return cannot record a gap** | Returns convert purchase units like receipts do, but have nowhere to record a conversion gap — so **gap reasons are silently discarded on that path**. "Never blocks" holds; the audit trail does not. | **M6** — contract change (nullable `grn_id`, or a return-side gap shape) | T2, 2026-08-29 |
| **Yield-at-receipt creates a permanent phantom variance** | `yield_factor_ppm` is applied at receipt, so the ledger holds USABLE quantity while a physical count counts GROSS on the shelf. Every yielded item shows a standing variance that is arithmetic, not shrinkage. Followed the field-consumer note deliberately; M4's variance report may need to know. | **M6** — decide whether variance reporting compensates, or yield moves | T2, 2026-08-29 |
| **`formatMicroQuantity` should print the largest readable unit — and the two quantity surfaces must NOT converge** | **The change:** `200000g` does not read at a receiving door. Stock arrives in kg and sacks, and `200000` vs `20000` is one glance apart where **200 kg vs 20 kg is not**. Rule: **kg above 1000 g, L above 1000 ml**, symbols retained. **DECIDED, NOT AN INCONSISTENCY — do not harmonise these later.** The two surfaces do different jobs and their conventions follow from that: **`entryIntentEcho`** is read ONCE, at the moment of commitment, by someone about to be wrong — spelled words and grouped digits, **deliberately hard to skim**, and it stays exactly as it is. **`formatMicroQuantity`** is a SCANNING surface — thirty rows whose magnitudes are compared at a glance — so symbols and the largest readable unit; spelled words would make it unusable. A future reader will notice `5,000 millilitres` beside `200000g` and reach for consistency. **That is the wrong instinct here, and this row exists to stop it.** **THE TRAP, which outlives the rule:** `entryIntentEcho` must keep doing NO ARITHMETIC beyond grouping — it restates what was TYPED. Re-deriving the edge's conversion in TypeScript is how the two drift, and **the edge is the authority on quantity exactly as it is on money**. The receiving echo's converted figure already comes from the edge via `grn_entry_intent_echo`; TypeScript only formats it. A "helpful" TS-side conversion would reintroduce the second-implementation defect the design forbids. **Own commit, own falsification:** `formatMicroQuantity` was just corrected for the 1000x VOLUME bug and its neighbours still carry that history — the last function to change casually. | **M6** — filed during the M5 acceptance pass, explicitly NOT fixed underneath it | This session, 2026-08-30, from the receiving-echo observation |
| **`check-contract-field-consumers.mjs` keys by FIELD NAME, not `table.field`** | It builds `name -> Set(file)`, so a field is "consumed" if **any** table's field of that name is read anywhere. `supplier.created_at`, `updated_at` and `schema_version` are dropped on the floor at the edge while the check reports **0 unconsumed** — masked by `invoice.created_at` and friends. This is the third defect found in this one check (after corpus width and enum blindness) and the most misleading: it reports a clean bill for a field nothing reads. | **M6**, with the guard-rail track — do the corpus narrowing first, same ordering rule | This session, 2026-08-30 |
| **`supplier.created_at` / `updated_at` / `schema_version`: on the wire, no column** | Sent by the cloud, silently ignored by the edge. **Ruling: `created_at` and `updated_at` come OFF the wire** — `config_version` is already the staleness signal and the upsert guard uses it, and the `app_user.created_at` exemption records the same reasoning. **`schema_version` stays, but must be CHECKED rather than stored**: a per-row copy is useless, and ignoring it means a version mismatch goes undetected, which is the whole reason the field exists. Note the edge ignores `schema_version` on several other config families too, so the check is cross-family, not supplier-specific. | **M6** — land after the acceptance pass, not during it | This session, 2026-08-30 |
| **`payment_allocation` assumes one payment settles one invoice** | A tender spanning two invoices is unmodelled. Money-shaped, so it waits for the milestone that touches supplier/settlement money. | **M7** | RESUME §6 |
| **`PAID_IN`/`PAID_OUT` emit no outbox event** | They ride inside `CashShiftOpened`/`Closed`. Visibility latency, not a money defect. | **M7** | RESUME §6 |
| **Supplier accounts posting, credit application, settlement** | `supplier_invoice`/`supplier_credit` are modelled at 0.6.0 and only created/listed in M5. `status` accepts `RECEIVED` only. | **M7** | ADR-019 §8 |
| **Row-level security / cross-tenant isolation in the database** | Isolation rests on per-query convention, not the database. One missing `WHERE tenant_id` is a cross-tenant read. | **M8, OR BEFORE A SECOND PRODUCTION TENANT — whichever comes first** | `backlog-m2.md` Deferred-with-trigger |
| **`TRANSFER_IN`, goods-in-transit, central kitchen `semi_finished_batch`** | A transfer spans two edge databases; half-building it is worse than not. `destination_outlet_id` is recorded now so no migration hunts for it later. | **M8** | ADR-019 §8 |
| **`DEFAULT gen_random_uuid()` retrofit — 8 columns in `postgres/0001_init.sql`** | §74 requires app-generated UUIDv7/ULID. A migrations lint holds the line (the count may only decrease), so this cannot get worse. | **M8** | `backlog-m2.md` Deferred-with-trigger |
| **Config deletion is unrepresentable across every delta-synced join table** | Every `*Since` export ships per-row deltas with no tombstone, so a removed row never reaches an edge. | **M8** | `backlog-m2.md`, found by T4b/T4c |
| **Device enrollment flow — HARD TRIGGER: blocks any pilot** | The contract shapes exist (ADR-017, 0.4.3); no operator-facing flow does. `scripts/dev-bootstrap.ps1` step 3b now enrols (or rotates) a dev till's credential over the real routes, which is a DEV FIXTURE and not that flow. It exposed one blocker for the real one: there is **no device LIST route**, and the 409 body does not carry the device id, so the bootstrap finds it in Postgres — an operator-facing flow cannot. See also the `outlet.manage` split row below, which this route sits behind. | **Trigger: any pilot deployment** | `backlog-m2.md` Security |
| **Repository is PUBLIC** | **DECIDED 2026-08-31: stay public. Do not re-raise without new facts.** Private was agreed and then reversed the same session, because removing the hardcoded AES key default (`e024f7f`) took away the one item with teeth. What remains public is architecture and roadmap — a competitor reads nineteen ADRs to exploit it and still cannot ship — plus a weak-points list that only matters to someone who can reach a running outlet, and none exists. **Against that, going private COSTS a proven backstop:** the `main` ruleset (`deletion`, `non_fast_forward`) is the remote half of the git-destruction defence behind three incidents, one costing 4,100 lines, and the local deny-list has a known prefix hole with nothing behind it if the ruleset goes. Rulesets on private repositories need Team; classic branch protection on private needs Pro; this account is Free. Free CI minutes are a bonus, not the argument. **Do not "flip it to find out" — that spends the backstop while you look, and the answer is documented.** | **Trigger: before the first pilot deployment, OR when the account moves to Team — whichever comes first** | This session, 2026-08-31 |
| **SPLIT `outlet.manage` — IT HAS BECOME A DE-FACTO ADMIN ROLE** | Not one hole with several routes in: one grant now gates **three unrelated capabilities** — table/outlet config, the compliance and GSTIN config writes, and **hardware enrollment** (`POST /devices/enroll`, ADR-017). Each was found separately, months apart, which is the tell. So whoever may rename a table may set the GSTIN printed on every invoice AND register a device that authenticates to the sync API. **Adding a `device.enroll` member fixes the instance and leaves the pattern intact, and a fourth capability lands behind it in M6.** The unit of work is the split: enumerate what `outlet.manage` actually gates today, give each its own permission, and let T7a's `billing.manage` be the first slice rather than the whole remedy. | **M6** — before any pilot, since enrollment is a pilot gate | T7a; second route found 2026-09-01 wiring ADR-020's credential |
| **Do NOT make the login rate limiter distinguishable from a wrong password** | Standing decision, recorded because the fix is tempting and is a security regression. ADR-012 returns the **identical** `authentication required` for a throttled attempt and a bad credential, on purpose: a distinguishable throttle response is an account-enumeration oracle, and it also tells an attacker exactly when to back off. The cost is real — five attempts per fifteen-minute in-memory window is easy to trip by re-running the bootstrap, and the 401 then reads as broken credentials. **Pay it in the CLIENT:** `dev-bootstrap.ps1` prints the rate-limit hint on any 401 and says a backend restart clears the window. Any future "helpful" 429 or throttle hint on this route is the wrong layer. | **Decided — do not re-raise** | 2026-09-01, wiring ADR-020's credential |
| **Redis-backed rate limiter** | The login limiter is in-memory: it does not survive restart and shares no budget across instances. | **Trigger: more than one API instance** | `backlog-m2.md` Security |
| **Edge DB key provisioning via `HOLLER_DB_KEY_HEX`** | Fail-fast on absence is right; an environment variable is not key management. **As of 2026-08-31 that premise is finally true on every path**: `dev-bootstrap.ps1` carried a hardcoded 64-hex default so re-runs were reproducible, which meant an outlet could ship with an edge database encrypted under a key published in this repository, because nobody had to choose one. **A default IS consent by omission** — the same shape as "absence is never consent", pointed at a key. Removed; the script now refuses to start a container without one. Do not reinstate a default "just for dev". | **Trigger: first real deployment** | `backlog-m2.md` Security; default removed this session |
| **MSI/WiX installer** | Dropped, not failed — two half-verified installers are worse than one verified one. Returns with its own clean-VM verification, never by flipping `targets` back to `"all"`. | **Trigger: a deployment requiring MSI** (Group Policy / SCCM, or IT that will not run NSIS) | `backlog-m2.md` Deferred-with-trigger |
| **Air-gapped build machine** | The WebView2 offline package is fetched from `go.microsoft.com` at *build* time. The installer is offline; the build is not. | **Trigger: a build host without internet** | `backlog-m2.md` Deferred-with-trigger |
| **Rust binding for `packages/contracts`** | Until a fourth Rust consumer exists, `check-event-type-drift.mjs` greps literals instead. | **Trigger: a fourth Rust consumer** | `backlog-m2.md` Contracts |
| **USB and Bluetooth printing unproven on hardware** | Only `NetworkTransport` (TCP) is exercised against a real listener. | **Trigger: an outlet using either** | `backlog-m2.md` M2 execution |
| **ESC/POS on paper** | An **M3 exit gate**. The file-sink transport proves the byte stream, not that a device accepts it. No printer exists in this environment; one is being sourced. | **PARKED — revisit ~2 September 2026** | ADR-013 / M3 acceptance |
| **Bare 4GB Windows 10 VM run** | An **M3 exit gate** and the ADR-013 validation. The installer half is done (`bundle.windows`, offline WebView2, static CRT, NSIS-only); the VM run needs a machine nobody has provisioned. | **PARKED — revisit ~2 September 2026** | ADR-013 addendum |
| **Seeded reorder levels are placeholders** | 28 of 32 items read LOW, so the banner trains people to ignore it. Data, not code. | **Trigger: any rollout or demo** | RESUME §2(d); M4 manual pass |
| **`menu_item.hsn_sac` is NULL on every existing edge database** | The edge **rejects invoice issuance** on a NULL/blank code, so no outlet can bill until its catalogue is configured. Correct and deliberate; it is a rollout step, not a defect. | **Trigger: any rollout** — operational gate | RESUME §6 operational gate |
| **An outlet with no `BILL`-role printer cannot print** | `print_invoice` fails loudly by name rather than queueing into nothing. Same shape as the HSN gate. | **Trigger: any rollout** — operational gate | RESUME §6 operational gate |
| **`PreToolUse` hook matching every spelling of a denied git command** | The prefix matcher in `.claude/settings.json` catches the common forms and misses flag-trailing ones. A `PreToolUse` hook on `Bash` sees the full command string and can match anywhere in it, closing the gap locally the way branch protection closes it remotely. **Deliberately not built yet: the deny-list plus branch protection covers the realistic surface, and a hook is machinery to maintain.** | **Trigger: the first denied command that gets through in a form the prefix missed.** Not before — that event is the evidence the cheap fix was insufficient | This session, 2026-08-29 |
| **Nine CI jobs still pin Node 20** | `lan-integration` and `e2e-scenario` were bumped to Node 22 on 2026-08-31 because both need Node's global `WebSocket`, unflagged only from 22. The other nine jobs pass on 20 and **were deliberately left alone rather than swept** — a nine-job bump is a change worth making on purpose, with a reason, not as a side effect of fixing two unrelated jobs. The cost of leaving them: a suite can adopt a Node 22 global and fail in CI only, exactly as `lan-integration` did for eleven days. | **Trigger: the first job that needs a Node 22 global.** Bump that job, or bump all nine at once as its own commit — not before | This session, 2026-08-31; the CI sweep |
| **`backend/internal/{auth,menu,tables}` never call `postgres.Migrate`** | They seed into an assumed schema; CI works around it with a `go run ./cmd/devseed` step. | **M6** | RESUME §6 |
| **`edge/sync/src/config.rs`: empty `device_credentials` is not an error** | Unlike empty `users`. "None enrolled" and "cloud forgot" are indistinguishable. | **M6** | RESUME §6 |
| **`{OUTLET}` numbering token derives from `outlet.name`** | No `outlet.code` exists in the frozen contract, so a rename changes invoice numbers. | **M6** | RESUME §6 |
| **`reset_policy` without a matching date token yields duplicate invoice numbers** | Caught by the UNIQUE index at issue time; not validated at config-write time, which is where a human could still fix it. | **M6** | RESUME §6 |
| **Display-number reset buckets by UTC day** | `edge/database/src/repo.rs`. The same UTC-vs-business-day split T7b fixes for `business_date_from`; this is the second caller. | **M6** — after T7b lands the shared function | RESUME §6 |
| **Three `unwrap_or`-on-a-parse defects in `edge/`** | Each substitutes a plausible valid value for an invalid stored one, so corruption reads as data. | **M6** | `backlog-m2.md`, directed sweep |
| **A mixed order sends silently when one line has no station** | Found on ~30% of randomized e2e runs and confirmed in production code. | **M6** | `backlog-m2.md` Correctness |
| **The e2e harness's CI job cannot go red on an invariant failure** | `scenario.test.ts:32` asserts only that the harness ran. An invariant failure is reported and passes. | **M6** | `backlog-m2.md` Correctness |
| **Tauri commands enforce no backend permissions** | Authorization on the POS is frontend-only gating; `confirm_order` and every kitchen command trust the caller. | **M6** | `backlog-m2.md` M2 execution |
| **Gaps screen: title and rows say "no recipe" for every gap reason** | Six reasons render as one sentence, so a `DIMENSION_MISMATCH` reads as a missing recipe. M5 adds eight more gap reasons on the inbound side. | **M6** | `backlog-m2.md` gaps screen, 2026-08-27 |
| **Timestamps render as raw UTC ISO on manager-facing screens** | The outlet timezone is stored and not used at the point a human reads the number. | **M6** | `backlog-m2.md` gaps screen |
| **`chrono-tz` — do NOT narrow with `filter-by-regex`** | Decided 2026-08-21 and recorded so nobody re-proposes it. Not a task; a standing decision. | **Decided — do not re-raise** | `backlog-m2.md` Deferred-with-trigger |
| **`pnpm test:e2e` (Playwright, KDS) is the one test step NOT behind the zero-execution guard** | Every other test step in `ci.yml` now runs through `scripts/assert-tests-ran.mjs`, which fails when a command executes zero tests. Playwright's summary format is not one of the three the guard parses, so that step is still able to pass having run nothing — `--grep` matching no title exits 0, as the other three runners do. Add a Playwright counter to the guard (`N passed` / `no tests found`), or the step keeps the property the guard was written to remove. | **M6** | Zero-execution audit, 2026-09-02 |
| **M6 criterion 2 (platform stock-out snooze) is PARKED — no platform sandbox access exists** | Swiggy and Zomato partner sandboxes need a signed partnership this project does not have. ONDC was probed rather than assumed on 2026-09-02: `preprod.registry.ondc.org` answered **403**, three other staging/gateway hosts did not answer at all — and **answering is not access**, since the ONDC registry requires a subscribed, whitelisted participant with a signed key pair and entity details. **Do not evidence a snooze from our own log: that is the definition of a criterion that cannot fail**, and it is the third instance this year of the family M5's retro named. Same register as the ESC/POS hardware gate. **M6 closes without this criterion rather than with a fake pass.** If access is granted mid-milestone, observe it on ONDC and name it ONDC-only in the record. | **Trigger: when platform sandbox access is granted** | M6 planning, 2026-09-02 |
| **ONDC Network Participant CERTIFICATION — a named gate, not a milestone** | Certification is a review queue with round-trips on log verification, 2–8 weeks calendar, **and its duration is not ours to set**. It is therefore outside M6 and outside M6.1: a milestone whose close depends on someone else's queue is a milestone that stops closing (the ESC/POS precedent). Prerequisites are Phase D's paperwork — registered legal entity, whitelisted domain, valid SSL certificate, public HTTPS callback host — plus a working signed implementation, which is M6.1's deliverable. **A live channel on ONDC is what certification buys; neither M6 nor M6.1 delivers one, and no report may imply otherwise.** | **Trigger: M6.1 complete and the NP application submitted** | M6 planning, 2026-09-02 |
| **THERE IS NO CLEAN EXIT PATH IN THIS BUILD, SO THE PLAINTEXT EDGE DATABASE WITH CREDENTIAL HASHES LANDS ON DISK ON EVERY SHUTDOWN** | ADR-011 requires the edge SQLite file to be encrypted at rest, and `edge/database/src/crypto.rs` documents the one window where it is not: while a `Db` is open, the decrypted bytes exist as a real file because WAL mode needs one. `Db::close`/`shutdown_in_place` checkpoints, reseals into `<name>.db.enc` and wipes the plaintext plus its `-wal`/`-shm` siblings. **A crash never reaches that code, so the file simply stays until that database is opened again.** **CORRECTION, 2026-09-07, same day this entry was written:** the original wording here said "nothing on the next start cleans up after the last one", and that is FALSE. `crypto::recover_crash_leftovers` exists and does the right thing — it opens the leftover plaintext, lets SQLite replay the WAL, checkpoints TRUNCATE, reseals that merged state into `<name>.db.enc` **superseding the older sealed file**, then wipes the plaintext and clears the marker. No committed row is lost. The claim was written from the observed artefacts without reading that function, which is the same mistake this repository keeps paying for in the other direction. **The exposure that remains is real but narrower than filed:** recovery runs only when that database is opened again, so the plaintext — carrying Argon2id `password_hash` and `pin_hash` — sits unencrypted for however long that takes. Observed spans of two days (5 to 7 September) and, for two M5 scratchpad artefacts never reopened, six and seven days. The fix is therefore not recovery-on-open, which exists; it is that nothing reseals a database whose process has died while the machine keeps running. Observed three times, each after an abnormal exit, each with the `edge.db.open-marker` still present: `%APPDATA%/com.holler.pos/edge.db` (2026-09-05, 1318912 bytes, with a `-wal` 13 minutes NEWER than the `.enc`, so the sealed file was also stale), and two M5 acceptance artefacts from 2026-08-31 and 2026-09-01. **Those two were deleted on 2026-09-07 by operator decision, so their identity is recorded here and nowhere else** (sha256, size, mtime): `crit2/edge.db` `680ba972c5ffc7707f669ff6248dbca72e8cce1470dfb338aa437cc408b7bad9`, 1286144 bytes, 2026-08-31 14:23:18 +0530; `crit2-positive/edge.db` `9d305888f795921d4a1d5a95b816549aa0bbd37f985c87c46374cb2e7abf2467`, 1286144 bytes, 2026-09-01 16:19:25 +0530; and the 32-byte edge key that opened them, `acceptance-key.txt` `7edbdaecaab360953850dab6755722be211150d216fb35428826e1cbe651144c`, 66 bytes, 2026-08-31 10:48:26 +0530, deleted with them. Both database files carried `edge.db.open-marker`, which is the observation this entry rests on. The exposed rows include the cached Argon2id `app_user.password_hash` and `pin_hash` that exist precisely so a cashier can log in offline. **UPGRADED 2026-09-10, and the title changed with it.** This was filed as an UNCLEAN-exit problem. It is not. During the M6 C1/C6 sitting the POS was stopped with `Ctrl+C` in its launching terminal -- the documented correct way -- and the seal did not run either: `edge.db`, `edge.db-wal` and `edge.db.open-marker` were all still on disk with the process gone, while `edge.db.enc` carried the timestamp of the moment the app OPENED. Taken with the window-close observation of 2026-09-07, **neither exit this build offers fires `RunEvent::Exit`**, so there is no path a user can take that seals the database. That makes this not an edge case after a crash but the ORDINARY OUTCOME OF EVERY SHUTDOWN: the plaintext SQLite file, carrying the cached Argon2id `password_hash` and `pin_hash` rows that exist so a cashier can log in offline, is on disk after every session and stays there until that database is next opened -- overnight at an outlet, indefinitely on a machine that is retired or reimaged. **The fix direction is therefore an in-app quit that fires the exit hook (and a shutdown path that runs the drain and the seal before the process ends), NOT better crash recovery.** Recovery already works and is not the gap; the gap is that nothing normal reaches the sealing code at all. Note the second-order effect this run also demonstrated: the sealed file is then OLDER than the plaintext beside it, so a reader doing the careful thing gets stale data with nothing announcing the difference -- the M6 C6 acceptance read hit exactly that and had to be done in place. **The marker is the fix's own starting point** — the process already knows, on the next open, that the previous session did not close cleanly; what it does not do is treat a surviving plaintext file as something to reseal or wipe. Note the second-order effect too: the stale `.enc` means a reader who does the careful thing and decrypts the sealed copy gets a state OLDER than the plaintext beside it — until the next open, when recovery reseals and the two converge. | **Trigger: before the first pilot** | Found 2026-09-07 reading `sync_outbox_block` for the M6 C7 run |
| **`dev-up.ps1` RUNS THE BOOTSTRAP BEFORE IT STARTS THE BACKEND, SO A COLD STACK ALWAYS COMES UP WITH SYNC DISABLED** | The script's own order is: step 2 runs `dev-bootstrap.ps1`, step 3 starts the backend API (`dev-up.ps1:167` then `:175-177`). But the bootstrap's step **3b** enrols this till's sync credential by calling `POST /devices/enroll` on the API — which nothing has started yet. On a cold start it therefore ALWAYS prints `[3b/4] sync credential SKIPPED: no API at http://localhost:8080` and writes no device token, and the POS treats a partial credential set as no set: **it comes up with sync silently disabled**. It is not fatal and it is not loud, which is why it reads as normal startup. **This has now cost two acceptance runs.** The bootstrap is idempotent, so the workaround is to re-run it once the backend is listening — but a workaround everyone must remember is the defect, not the fix. **Fix direction:** the dependency is real and one-directional — 3b needs the API, the API needs Postgres migrated, and the bootstrap is what migrates it — so the sequence has to become bootstrap steps 1–3, start the backend, THEN 3b and step 4. That is a restructure across both scripts, which is why it was filed rather than done: it was found DURING an acceptance run, and changing the launcher mid-run risks the run it was found in. | **Trigger: before the first pilot** — and before the next acceptance run that depends on sync, whichever comes first | Found 2026-09-07, second occurrence, during the M6 C7 run |
| **M6 A4 - `Offline` conflates four distinct states (DEFERRED from Phase A)** | `Offline` is returned for: no listener on the port, a listener that refused the connection, a stale pooled socket, and a host that answered with an error. An operator and a log reader cannot tell a shop with its WAN down from a backend that is up and rejecting everything, and the two need opposite responses. The transport half landed at `262e03a`; **this is the reporting half only**, and it must reuse the three-probe fail-closed logic already in `scripts/check-cloud-unreachable.ps1` rather than growing a second, weaker version. **Deferred, not dropped:** Phase A closed on the five items that unblock the aggregator outbox, and this one degrades diagnosis rather than losing data. | **Trigger: before the first pilot** | M6 Phase A close, 2026-09-07 |
| **M6 A6 - the shutdown drain's output is lost in some builds, and closing the window does not exit the process (DEFERRED from Phase A)** | Two related observations. (a) `drain_outbox` reports through `eprintln` (`state.rs`), whose lines have been seen in some runs and not others; the work is to establish WHICH build/attach state loses them, which is a different fix from adding a log line. (b) **Found during the 2026-09-07 C7 run and folded in here:** closing the POS window under `tauri dev` does NOT terminate the process - `holler-pos.exe` PID 78528 stayed alive with the periodic pump still ticking and the database still open and unsealed, so `RunEvent::Exit` never fired and the shutdown drain never ran. A till that looks closed and is still syncing is the same class of invisible state A6 exists to remove. Whether this is dev-only or reaches a release build is the first question, and it is unanswered. **UPDATED 2026-09-10: it is not only the window-close case.** During the M6 C1/C6 sitting the POS was stopped with `Ctrl+C` in its launching terminal -- the documented correct way -- `Get-Process holler-pos` returned nothing, and the seal still did not run: `edge.db`, `edge.db-wal` (11:56:05) and `edge.db.open-marker` were all left behind while `edge.db.enc` carried 10:27:27, the time the app OPENED. So the sealed file did not contain that evening trading at all, the plaintext beside it did, and the acceptance read had to be done in place rather than by the sealed-copy method. Both exits now known to skip the drain and the seal. | **Trigger: before the first pilot** | M6 Phase A close, 2026-09-07; (b) observed during the C7 run |
| **M6 A7 - 78 outbox rows have no edge route and can never be sent (DEFERRED from Phase A)** | `edge/sync/src/route.rs` maps only `order` and `table_session`. Every other aggregate's rows are reported `unrouted_skipped` and sit pending forever - the drain counts them but nothing can send them. **Measured on the live edge database, 2026-09-07:** 78 pending rows with no route - **55 `kot`, 22 `stock_count`, 1 `invoice`** - alongside 24 pending `order` rows that DO have a route (13 distinct aggregates, of which 5 are blocked on `missing_reference`). The cloud ingest routes already exist, so this is an edge resolver and envelope job, not a backend build; it covers `kot`, `invoice`, `payment`, `cash_shift`, `stock_count` and item availability. **This is the largest single carry-forward out of Phase A** and the reason the pending-row count never reaches zero however healthy the drain looks. | **Trigger: before the first pilot** | M6 Phase A close, 2026-09-07 |
| **The cloud's copy of an order stops tracking the till after the create event — REPRODUCED TWICE, CAUSE UNKNOWN, A7 RULED OUT FOR THE SECOND INSTANCE** | First instance: order #A1 read `DRAFT` at ₹300.00 in Postgres while the till had it billed, printed and settled at ₹315.00 — attributed to A7, since `invoice` and `payment` have no edge route. Second instance rules that explanation out: order `01a08b38-91a6-78d0-b805-123e9ff75a10` reads two items and ₹385.00 on the till, and in Postgres is `DRAFT` at `5500` paise with a single `order_item` line whose `line_total_paise` is `22000` — a stored total agreeing with neither the till nor its own line. **`order` HAS an edge route**, so A7 does not explain this one and the finding is upgraded from "not investigated, A7 explains part" to "reproduced twice, cause unknown". What the one-query read established, and nothing more: the cloud holds 990 `menu_item` rows and the line's `menu_item_id` (`0191a000-0000-7000-8000-000000000012`, Veg Thali) exists, so a missing menu item is not the reason. A lead, untested: both rows named in the till's blocked-sync banner (`01a04210-...d14b3`, `01a04219-...14f8d1`, 5 attempts, `missing_reference` HTTP 422) are present in Postgres AS ORDERS, so what is blocked are later events on orders whose creates landed — consistent with item and status events failing while creates succeed. Note the create event itself is fine: the C4 order landed at `30000` paise against ₹300.00 on the till, exact. **Consequence if unfixed: every cloud-side report, reconciliation and admin screen reads a total that is neither what the customer paid nor what the outlet recorded, with nothing announcing the difference.** | **Trigger: before the first pilot** | M6 C3/C4 run, 2026-09-10 |
| **The periodic sync pump makes the till sluggish while the cloud is unreachable** | Observed 2026-09-10 with the cloud confirmed down by all three probes of `scripts/check-cloud-unreachable.ps1`: the operator reported the application slow while ringing an order. Every tick takes the database lock and walks the pending backlog against a dead uplink, and the UI waits behind it. **ADR-013's promise is that an outlet is unaffected by a dead uplink**, and a pilot outlet with no connectivity is the normal case, not the exception — so this is the ADR's own guarantee failing under the condition it was written for. Severity as shipped is UNMEASURED: the run used `HOLLER_SYNC_PUMP_INTERVAL_SECS=10` so a tick could be watched inside one sitting, six times the shipped 60-second cadence, and the backlog was ~100 rows. Measure at the default interval before deciding a fix. Directions, none chosen: hold the lock for a shorter span, back off the interval while the uplink is known dead (A4's reporting half would say when), or bound the walk further than `DRAIN_BATCH_LIMIT` already does. | **Trigger: before the first pilot** | M6 C4 run, 2026-09-10 |
| **The POS Orders screen renders raw UTC in the Created column** | Shows `2026-09-10T13:33:20.733Z` for an order rung at 19:03 local, which an operator reads as a clock fault — it was reported as one during the C4 run before being identified. CLAUDE.md's rule is UTC storage with the outlet timezone stored separately and rendered local. Cosmetic and carries no data implication, but it is on the screen an operator uses most, and it makes every other timestamp on that screen unreadable at a glance. | **Trigger: before the first pilot** | M6 C4 run, 2026-09-10 |
| **The sync banner prints `aggregate_id`, so one order appears once per outbox row** | Observed on the shipping binary during the 2026-09-07 C7 run: a four-item order produced three separate entries all reading `order 01a04210-...`, because they are three distinct outbox rows (`01a04210-a428-...`, `01a04210-a6f6-...`, `01a04211-d7f2-...`, all `ItemAdded`) of one aggregate. Once a row exhausts its budget and is abandoned, the drain moves to the next row of the same aggregate, which spends its own five attempts and blocks ~30s later. **Two consequences:** the operator reads a climbing count - toward ~20 entries for what is 5 orders - as that many lost orders; and fully surfacing one order costs five attempts PER EVENT rather than five in total. **The open design question is whether successor rows of an already-abandoned aggregate should be attempted at all**: the predecessor is abandoned, so ordering is already broken, and each successor spends a full budget to rediscover the same rejection. | **Trigger: before the first pilot** | M6 C7 run, 2026-09-07 |
| **The sync banner covers the POS top bar** | `.sync-blocked-banner` is `position: fixed; top: 0` with content-dependent height. At seven rows it hid the search box and the whole DINE_IN / TAKEAWAY / DELIVERY / Select table / Orders / Stock row; the count grows as more rows block, so it gets worse over time. The CSS comment justifies the placement by saying it takes "a region nothing else occupies" - that region is the top bar's, and `.print-failure-banner` and `.stock-banners` had already been fixed once for overlapping each other, making this the third instance of the same collision. **Fix direction: reserve layout space rather than overlay** (the banner is not transient - a permanently-blocked row can sit there for days), or cap the visible rows with a count and a details view. | **Trigger: before the first pilot** | M6 C7 run, 2026-09-07 |
| **Admin console: PURCHASE ORDERS surface (DEFERRED from Phase B)** | Raising, amending, approving and listing purchase orders from the admin console. **Deferred because no M6 criterion touches it and Phase C does not depend on it:** C5 needs suppliers and pack sizes, C6 needs the goods-receipt list, and the aggregator menu push needs a menu surface that is not `devseed` - purchase orders sit outside all three. The domain work already exists behind it (`backend/internal/procurement`, `role.po_approval_limit_paise`, ADR-019), so this is a UI build, not a design question. Note the constraint that will govern it when it is built: **`purchase_order` carries NO receipt state, and the edge's and cloud's receipt progress LEGITIMATELY DIFFER** - a shared PO reads "40 of 100" at one till and "90 of 100" in the admin, simultaneously, and both are right. Show both and label them; never reconcile them. | **Trigger: before the first pilot** | M6 Phase B scope decision, 2026-09-07 |
| **Admin console: STAFF AND PERMISSIONS surface (DEFERRED from Phase B)** | Creating and editing staff, assigning roles, and managing the permission grants behind them. **Deferred from Phase B because no M6 criterion touches it and it is not a Phase C dependency - but it is PILOT work, not milestone work: an outlet cannot run unattended without staff management.** Today the only way to create a user is `devseed`, which means a real outlet would have exactly one cashier and no way to add, remove or lock out staff. Two existing backlog items belong to this work and should be done with it rather than separately: **the `outlet.manage` split** (one grant currently gates table/outlet config, the compliance and GSTIN config writes, AND hardware enrollment), and **Tauri commands enforcing no backend permissions** (authorization on the POS is frontend-only gating today). Building a staff screen on top of a permission model that the till does not enforce would produce a console that looks like access control and is not. | **Trigger: before the first pilot** | M6 Phase B scope decision, 2026-09-07 |
| **A menu item deleted in the cloud is never removed from an edge** | Found landing contracts 0.7.0 (ADR-024 §8). The config apply is `upsert_menu_item` per row -- `INSERT ... ON CONFLICT(id) DO UPDATE` guarded by `excluded.config_version >= menu_item.config_version` -- and NOTHING deletes rows absent from the bundle. So a withdrawn dish stays sellable at every till that ever saw it, indefinitely, and the admin console shows it as gone. There is no tombstone, no prune and no soft-delete flag on the path. **Deliberately not fixed as a rider on a route addition:** deletion semantics for cloud->edge config is its own decision (does a delete tombstone, soft-delete, or prune-by-absence? what happens to an open order holding the item? what does a till do with a KOT already printed for it?), and choosing a default while doing something else is how the wrong one gets set. Note the same question applies to every other cloud->edge config table, not just menu_item. | **Trigger: before the first pilot** | ADR-024, 2026-09-07 |
| **The cloud devseed inserts `menu_item` with NO `hsn_sac`, so every cloud menu row is unbillable** | `backend/cmd/devseed/main.go` inserts (id, outlet_id, category_id, name, base_price_paise, is_available, config_version) and NOT hsn_sac, while `edge/database/src/bin/devseed.rs` sets `hsn_sac: Some("9963")` on the same two items. So the cloud's copy of the dev catalogue carries NULL and the admin console correctly renders "not set - cannot be billed" for every item. **An invoice cannot legally issue with a NULL or blank HSN/SAC on any line (0.4.5)**, so this is not cosmetic: it is the cloud half of the operational gate already filed for the edge. Two seeders describing the same two items differently is also the drift the config-apply direction is supposed to remove. Fix is a one-line column addition in the Go seeder, deliberately not made while closing a phase on observed screens -- changing the seed changes what the screenshots showed. | **Trigger: before the first pilot** | M6 Phase B close, observed 2026-09-08 |
| **Aggregator order REJECT at the till (deliberately out of M6 C1's scope)** | The accept path landed on 2026-09-10 (`apps/pos/src-tauri/src/commands/aggregator.rs`): a document the till holds becomes a local `order` and bills on the existing path. **There is no reject.** A till that cannot take an order -- kitchen closed, item genuinely out, a duplicate the platform sent twice -- has no in-product way to say so, and the document stays on the pending list forever. **Not built because its shape is argued from what a real platform does with a refusal**, which is the one thing no fake we authored can tell us: does a reject need to reach the platform before it means anything, does the platform retry, does a rejected document come back with a new `document_version`? Guessing produces a local state the platform disagrees with, which is worse than having no button. | **Trigger: before any platform sandbox access** | M6 C1 scope decision, 2026-09-10 |
| **Platform-side CANCEL visibility at the till (deliberately out of M6 C1's scope)** | `aggregator_order` is replace-not-merge on `document_version` and the down-path pulls by `updated_at`, so a platform cancellation DOES reach the till as a new version of the document -- that machinery exists and is tested. What does not exist is anyone seeing it: once a document has been accepted it leaves the pending list (acceptance is derived from the local order's existence), so a cancelled-after-accept document updates silently underneath an order the kitchen is already cooking. The accept screen is the wrong place for it; the order screens are the right place, and what they should say depends on what a platform actually sends and when. **Same reason as reject:** the shape comes from real platform behaviour, not from our fake. | **Trigger: before any platform sandbox access** | M6 C1 scope decision, 2026-09-10 |
| **`apps/admin` mints ids with `crypto.randomUUID()`, which is UUIDv4, against §74** | `SuppliersScreen.tsx:114` and `:139`. §74 requires app-generated UUIDv7/ULID, and every supplier and `supplier_item` created from the console so far carries a v4 id -- the supplier created during the 2026-09-10 sitting is `4eed005a-8889-479f-b3a5-20fcb5d5fb2c`, version nibble 4. Nothing breaks today: the ids are unique and the FKs hold. What is lost is the time-ordering property v7 exists for, on exactly the rows a keyset pagination or a "what was created when" question would use. Fix is a UUIDv7 helper in the admin and a sweep of the existing rows' ordering assumptions -- not a rewrite of the ids already issued. | **Trigger: before the first pilot** | Found during the M6 C5 run, 2026-09-10 |
| **Admin supplier form collects no GSTIN, and the pack-size table shows a raw UUID** | Two smaller findings from the same screen on 2026-09-10. The create-supplier form has no GSTIN field at all, so every supplier the console creates renders "no GSTIN" -- which matters for input-tax-credit work, where a purchase without a supplier GSTIN is not claimable. And the pack-size table's Item column prints the raw `inventory_item_id` rather than the item's name, so the operator had to be handed a UUID to type. Neither blocks C5. | **Trigger: before the first pilot** | Found during the M6 C5 run, 2026-09-10 |
| **ADR-025 table ordering device (customer tab) — PROPOSED, not built** | Authority, principal kind, transport and the append-only rule are decided in `docs/adr/ADR-025-table-ordering-device.md` and summarised in `docs/architecture/SYSTEM_ARCHITECTURE.md`. The tab is a LAN client of the till like the KDS, never a cloud client, because orders are edge-authoritative and table ordering must survive a dead uplink. **Recorded so nobody reads the architecture section as work in flight**, and so the shape is not re-argued from scratch at M8 planning. | **M8 planning** — proposed landing M8, trigger *after the first pilot runs on `STAFF_ONLY`* | ADR-025, 2026-09-10 |
| **`order.source` must also name `TABLE_TAB`** | The same CHECK widening that names the aggregator platform: one change, two values, one ADR note, one consumer list (both stores, Go struct, Zod schema, OpenAPI, every writer). A tab-entered order recorded as `POS` is the same defect as an ONDC order recorded as `DIRECT` — a stored value that lies, wrong in every report that groups by channel. Doing it once is strictly cheaper than doing it twice, which is why this one item of ADR-025 is pulled forward out of M8. | **With the 0.8.1 change already pending — BEFORE M6 CLOSES.** On the M6 boundary list in `docs/m6-acceptance.md` | ADR-025, 2026-09-10 |
| **`table_device` principal kind, permission set, and enrolment bound to (outlet_id, table_id)** | Structurally narrower than any staff role: read menu and availability for its outlet, create and append to the order bound to ITS table, nothing else. **Enforced at the LAN boundary, not by the tab's UI** — a device in public hands is not a trusted client, and a permission that lives in the client is not a permission. **Needs a falsifying test that a tab reaching another table's order is REFUSED**, watched failing first: an isolation rule nobody has watched fail is not isolation. | **M8 planning** | ADR-025, 2026-09-10 |
| **Per-outlet `ordering_mode` and per-table `tab_enabled` in cloud config, with an admin surface** | `ordering_mode ∈ {STAFF_ONLY, TABLE_TAB}` per outlet, `tab_enabled` per table; cloud→edge config, delivered by the config pull on the A5 loop. **The edge refuses tab enrolment for a table that is not enabled** — a config only the admin console honours is not a control. Both modes must coexist inside one outlet: an outlet with six tabs and twenty tables is the ordinary case. | **M8 planning** | ADR-025, 2026-09-10 |
| **LAN availability read, and typed rejection of an unavailable item at the till** | The till already computes stock-out and low-stock; expose it as a LAN read so the tab renders what the edge says rather than keeping its own view. When an item goes unavailable between the tab showing it and the till receiving the order, the till rejects that item **with a typed reason the tab renders** — never silently accepted, never silently dropped, the same discipline as `missing_reference`. | **M8 planning** | ADR-025, 2026-09-10 |
| **`apps/table` scaffold from the `apps/kds` pattern** | A web app served from the till's LAN server and enrolled as a device. Fourth frontend; the cost is accepted because the alternative — a cloud-hosted tab — fails the offline requirement outright. | **M8 planning** | ADR-025, 2026-09-10 |
| **LAN security gate review for a PUBLIC-FACING device** | The M2 LAN socket security gate has only ever been considered for staff-side consumers (KDS, printer bridge). A tab is the first untrusted client on that transport: threat model for a device a member of the public holds, rate limiting, and a per-table binding that cannot be re-pointed from the tab itself. | **Trigger: before the first tab is enrolled anywhere** — earlier than M8 planning if a tab is piloted sooner | ADR-025, 2026-09-10 |
| **Customer-side payment from the tab — PARKED, no trigger** | Tab orders, staff bills, in the first version. Customer-side payment brings a payment provider, customer-initiated refunds and a device in public hands touching money; it is a separate decision with its own gate. Parked deliberately so it is not absorbed into the tab work by drift. | **PARKED — no trigger yet** | ADR-025, 2026-09-10 |
