# Visual verification evidence

Screenshots that close a row in `docs/visual-verify.md`.

Name a file for the row it closes, so the register and the folder cannot drift
apart: `VV-003-order-number-till.png`. One row may cite several files.

A row closes only with a file in here. "Seen in the dev server" does not close
a row, and neither does a passing test — see the register's rules.
